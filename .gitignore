# macOS가 폴더마다 자동으로 만드는 숨김 파일
.DS_Store

# Claude Code 로컬 설정 (다른 기기/사람과 공유할 이유 없음)
.claude/

# Netlify Drop용으로 만든 배포 사본 — GitHub Pages는 루트 파일을 바로 서빙하므로 필요 없음
dist/

# 원본 캡션/이미지 소스 — 공개 레포에 올리지 않음
workspace/
